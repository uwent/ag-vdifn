<div class='iw-title'>
  Grid Point Details (<%= @latitude %>&deg;N, <%= @longitude %>&deg;W)
</div>

<div id='iw-content'>
  <h3 class="weather-details-title"><%= @model_value %> (<%= @t_min %>/<%= @t_max %> &deg;<%= temp_symbol() %>)</h3>
  <% if @data.size > 0 %>
    <div class="information" style="padding-bottom: 1em;">
      <div>
        <b>Date range:</b> <%= @data.first[:date] %> to <%= @data.last[:date] %> (<%= @data.count %> days)
        <br><b>Degree-day total:</b> <%= convert_dds(@data.last[:cumulative_value]).round(1) %> <span style="font-size: smaller"><%= temp_symbol() %>DD</span>
        <% if @data.size >= 14 %>
          <br><b>14-day average:</b> <%= convert_dds(@data.last(14).inject(0) { |sum, h| sum += h[:value] } / 14.0).round(1) %> <span style="font-size: smaller"><%= temp_symbol() %>DD / day</span>
        <% end %>
        <% if @data.size >= 7 %>
          <br><b>7-day average:</b> <%= convert_dds(@data.last(7).inject(0) { |sum, h| sum += h[:value] } / 7.0).round(1) %> <span style="font-size: smaller"><%= temp_symbol() %>DD / day</span>
        <% end %>
        <% if @data.size >= 2 %>
          <br><b>2-day average:</b> <%= convert_dds(@data.last(2).inject(0) { |sum, h| sum += h[:value] } / 2.0).round(1) %> <span style="font-size: smaller"><%= temp_symbol() %>DD / day</span>
        <% end %>
      </div>
    </div>
    <div class="weather-details">
      <table>
        <thead>
          <tr>
            <th>Date</th>
            <th>Min Temp.</th>
            <th>Max Temp.</th>
            <th class="weather-details-dsv" data-balloon-length="small" data-balloon-pos="down" aria-label="Degree-days quantify the amount of heat energy generated each day that contributes to insect development.">
              Degree Days <div class="info-icon">?</div>
            </th>
            <th class="weather-details-dsv" data-balloon-length="small" data-balloon-pos="down-right" aria-label="Accumulated degree-day values can be used to estimate insect development.">
              Cumulative Degree Days <div class="info-icon">?</div>
            </th>
          </tr>
        </thead>
        <tbody>
          <% @data.each do |day| %>
            <tr>
              <td class="odd"><%= day[:date] %></td>
              <td><%= convert_temp(day[:min_temp]).round(1) %>&deg;<%= temp_symbol() %></td>
              <td class="odd"><%= convert_temp(day[:max_temp]).round(1) %>&deg;<%= temp_symbol() %></td>
              <td><%= convert_dds(day[:value]).round(1) %></td>
              <td class="odd"><%= convert_dds(day[:cumulative_value]).round(1) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <p style="text-align: center;">No data to display.</p>
  <% end %>
</div>
